function createArray(size) {
    const arr = [];

    for (let i = 0; i < size; i++) {
        arr[i] = i + 1;
    }

    return arr;
}

const arr = createArray(10);

// for (let i = 0; i < arr.length; i++) {
//     console.log(arr[i]);
// }

// for (let i = arr.length - 1; i >= 0; i--) {
//     console.log(arr[i]);
// }


// for (let i = 10; i > 0; i--) {
//     console.log(i);
// }

// ДЗ
// написать функцию

function findIndex(arr, elementToFind) {
    // magic

    return // индекс найденного или -1
}